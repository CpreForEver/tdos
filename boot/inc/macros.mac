
MACRO PRINT MSG {
	IF ~ MSG EQ SI
		PUSH SI
		MOV SI, MSG
	END IF

	CALL PRINT_STRING

	IF ~ MSG EQ SI
		POP SI
	END IF
}

MACRO GET_REAL {
	use32
		call near SET_REAL_MODE
	use16
}

MACRO GET_PROT {
	use16
		call near SET_PROT_MODE
	use32
}

